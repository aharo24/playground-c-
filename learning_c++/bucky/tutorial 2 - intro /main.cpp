#include <iostream>
// preprocessor directive --> #include <iostream> // we are going to be including a file that we will be using later that we did not make which is an i/o stream

using namespace std; // we are going to be using all the standart c++ stuff/libs

int main () // main is the first thing the program knows to execute | int says return --> int
{
    //cout --> output stream object
    //<< stream insertion operator
    //endl --> end the line
    cout << "hello world (:" << endl;   // TABS, spaces in C++ dont mean anything
    return 0; // each instruction ends w/a ';'
}


